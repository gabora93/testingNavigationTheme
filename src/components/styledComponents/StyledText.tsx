import {Text, styled} from '@gluestack-ui/themed';

const StyledHS = styled(Text, {
    fontWeight: "$extrabold",
    fontStyle: "normal",
    fontSize: "$lg",
    color: "white",
    _dark: {
      color: "white",
    },
  })

  export default StyledHS;