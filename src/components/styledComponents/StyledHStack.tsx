import {HStack, styled} from '@gluestack-ui/themed';

export default styled(HStack, {
    justifyContent: "center",
    height: '$16',
    backgroundColor: "#1F4F7B",
    _dark: {
      backgroundColor: "#1F4F7B",
    },
  });